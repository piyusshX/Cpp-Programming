#include <iostream>
using namespace std;
int main(){
    int a;
    if(3+2%5)
      cout<<"this works"<<endl;
    if(a=11)
      cout<<"even this works"<<endl;
    if(-7)
      cout<<"WTFFFFF";
    // reason is simple in case of if condition every thing other than false/0/""/null
    // will be treated as true
}
